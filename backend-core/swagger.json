{
	"openapi": "3.0.0",
	"paths": {
		"/groups/{groupSlug}/projects/{projectSlug}/checks/{checkSlug}": {
			"get": {
				"operationId": "StatusRecordController_readHistory",
				"parameters": [
					{ "name": "groupSlug", "required": true, "in": "path", "schema": { "type": "string" } },
					{ "name": "projectSlug", "required": true, "in": "path", "schema": { "type": "string" } },
					{ "name": "checkSlug", "required": true, "in": "path", "schema": { "type": "string" } }
				],
				"responses": {
					"200": {
						"description": "",
						"content": { "application/json": { "schema": { "type": "array", "items": { "$ref": "#/components/schemas/StatusRecord" } } } }
					}
				},
				"tags": ["status-record"]
			}
		}
	},
	"info": { "title": "Core", "description": "", "version": "1.0.0", "contact": {} },
	"tags": [],
	"servers": [{ "url": "/api/core", "description": "Current installation" }],
	"components": {
		"securitySchemes": { "bearer": { "scheme": "bearer", "bearerFormat": "JWT", "type": "http" } },
		"schemas": {
			"StatusRecord": {
				"type": "object",
				"properties": {
					"groupSlug": { "type": "string" },
					"projectSlug": { "type": "string" },
					"checkSlug": { "type": "string" },
					"time": { "format": "date-time", "type": "string" },
					"statusCode": { "type": "number" },
					"latency": { "type": "number" },
					"data": { "type": "object" },
					"_id": { "type": "string" },
					"uuid": { "type": "string" },
					"createdAt": { "format": "date-time", "type": "string" },
					"createdBy": { "type": "string" },
					"changedAt": { "format": "date-time", "type": "string" },
					"changedBy": { "type": "string" }
				},
				"required": [
					"groupSlug",
					"projectSlug",
					"checkSlug",
					"time",
					"statusCode",
					"latency",
					"data",
					"_id",
					"uuid",
					"createdAt",
					"createdBy",
					"changedAt",
					"changedBy"
				]
			}
		}
	}
}
